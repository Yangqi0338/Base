<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 迁移自 new-scm/scm-sale OrderDAO.xml; 适配: id -> order_no, 补 close_reason 条件赋值(Base 签名携带) -->
<mapper namespace="com.newzkl.platform.base.biz.order.infrastructure.dao.OrderDAO">

    <!-- 源 updateOrderState(by id) 的 orderNo 适配版 -->
    <update id="updateOrderStateByOrderNo">
        update `order` t set t.order_state = #{toState,jdbcType=INTEGER},
        <if test="closeReason != null">
            t.close_reason = #{closeReason,jdbcType=VARCHAR},
        </if>
        t.order_state_log = CONCAT(t.order_state_log, ',', #{toState,jdbcType=INTEGER})
        where t.order_no = #{orderNo,jdbcType=VARCHAR} and t.order_state = #{sourceState,jdbcType=INTEGER}
    </update>

</mapper>
