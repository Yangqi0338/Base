<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.newzkl.platform.Base</groupId>
        <artifactId>biz-order</artifactId>
        <version>${base.revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>biz-order-infrastructure</artifactId>
    <packaging>jar</packaging>
    <name>biz-order-infrastructure</name>
    <dependencies>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-order-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>ddd-infrastructure</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>core-utils</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-finance-facade</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-account-facade</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-sys-facade</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-goods-facade</artifactId>
        </dependency>
        <!-- 迁移补充: 仓储实现使用 TransferUtils/BizUtil/generator (core-utils) -->
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>core-utils</artifactId>
        </dependency>
        <!-- 迁移补充: 订单缓存 RedisUtil (core-redis) -->
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>core-redis</artifactId>
        </dependency>
        <!-- 迁移补充: 发送 MQ (MQUtil) -->
        <dependency>
            <groupId>com.newzkl.platform.Base.core</groupId>
            <artifactId>core-mq-infrastructure</artifactId>
        </dependency>
        <!-- 跨域出站: OperatorApi 经 OperatorFacade 调账户域运营商能力 (只依赖 facade 契约, 不碰 biz-account 内部) -->
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-account-facade</artifactId>
        </dependency>
    </dependencies>
</project>
