<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.newzkl.platform.Base</groupId>
        <artifactId>biz-finance</artifactId>
        <version>${base.revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>biz-finance-infrastructure</artifactId>
    <packaging>jar</packaging>
    <name>biz-finance-infrastructure</name>
    <dependencies>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-user-facade</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-order-facade</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-course-facade</artifactId>
        </dependency>

        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-finance-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>ddd-infrastructure</artifactId>
        </dependency>

        <dependency>
            <groupId>com.newzkl.platform.Base.core</groupId>
            <artifactId>core-mq-infrastructure</artifactId>
        </dependency>
        <!-- 迁移补充: 渠道商/供应商购买席位跨域取套餐 (SeatPackageApi 适配器经 biz-goods-facade, 席位套餐属虚拟商品) -->
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-goods-facade</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-account-facade</artifactId>
        </dependency>
        <!-- 跨域出站: DictApi 经 IDictFacade 调 biz-sys 字典能力 (只依赖 facade 契约) -->
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-sys-facade</artifactId>
        </dependency>
        <!-- 迁移补充: 仓储实现使用 TransferUtils/BizUtil/generator (core-utils) -->
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>core-utils</artifactId>
        </dependency>
        <!-- 迁移补充: 配置/提现缓存 RedisUtil (core-redis) -->
        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>core-redis</artifactId>
        </dependency>

        <dependency>
            <groupId>com.newzkl.platform.Base</groupId>
            <artifactId>biz-store-facade</artifactId>
        </dependency>
    </dependencies>
</project>
