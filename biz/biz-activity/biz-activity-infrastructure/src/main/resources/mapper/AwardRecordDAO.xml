<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newzkl.platform.base.biz.activity.infrastructure.dao.AwardRecordDAO">

  <insert id="recordAwardOrder" parameterType="java.util.List">
    insert into award_record (id, channel_id, activity_id,
      strategy_id, channel_activity_id, award_state,
      grant_type, member_id, award_id,
      award_type, ext_info, create_time)
    values
    <foreach collection="list" item="item" index="index" separator=",">
      (
      #{item.recordId,jdbcType=BIGINT}, #{item.channelId,jdbcType=BIGINT}, #{item.activityId,jdbcType=INTEGER},
      #{item.strategyId,jdbcType=BIGINT}, #{item.channelActivityId,jdbcType=BIGINT}, #{item.awardState,jdbcType=INTEGER},
      #{item.grantType,jdbcType=INTEGER}, #{item.userId,jdbcType=BIGINT}, #{item.awardId,jdbcType=BIGINT},
      #{item.awardType,jdbcType=INTEGER}, #{item.ext,jdbcType=VARCHAR}, NOW()
      )
    </foreach>
  </insert>
</mapper>
